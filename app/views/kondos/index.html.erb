<!-- Delete me later!-->
<% if current_user.nil? %>
  <h1> Kondo Index Page (NO current_user)</h1>
  <% elsif current_user.renter? %>
    <h1> Kondo Index Page (Renter)</h1>
<% elsif current_user.provider? %>
  <h1> My Kondos (Provider) </h1>
<% end %>
<!-- Delete h1 later !-->
<div class="container">
  <div class="row">
    <% @kondos.each do |kondo| %>
      <div class="col-xl-3 col-md-6 col-sm-12 d-flex justify-content-center">
        <%= render "components/card",
          photo: kondo.user.user_photo,
          name:  kondo.user.first_name,
          price: kondo.price,
          summary: kondo.summary.split(" ").first(10).join(" "),
          kondo: kondo.name,
          prefecture: kondo.prefecture,
          path: kondo_path(kondo)
        %>
        </div>
    <% end %>
  </div>
</div>
